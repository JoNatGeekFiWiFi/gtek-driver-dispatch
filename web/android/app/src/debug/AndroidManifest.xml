<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only overlay: allow plain-HTTP API calls so the emulator can reach
     the dev server on the host (10.0.2.2). Release builds stay HTTPS-only. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
